1. Bd en 'competitor' valor por defecto en 'active_user' deberia de ser 'false'
2. Falta en user un apartado q se llame code o algo asi, para verificar que ese codigo de activacion es el mismo que se paso por email.
3. Register_data en competitor, timestamp??¿ por defecto¿?
4. Añadit a db a la tabla de hackathon un campo que sea especifico para ese hackathon. Tipo un identificador para comprobar si exixte uno
parecisdo en la base de datos.
5. Uso de transactions
6. Falta tematica en el hackaton en sql


SELECT first_name, last_name, subsidiary_id, employee_id
  FROM employees
 WHERE ( subsidiary_id    = :sub_id OR :sub_id IS NULL )
   AND ( employee_id      = :emp_id OR :emp_id IS NULL )
   AND ( UPPER(last_name) = :name   OR :name   IS NULL )


# State run
NODE_ENV=development
# Server
PORT=3000
DB_HOST=localhost
# Database
DB_USER=
DB_KEY=
DB_NAME=portal_hackathones
# Admin root
ROOT_NAME= root
ROOT_PASSWORD= rootMaster21
ROOT_KEY_INIT= BKRLEODJEkgk5fdk332dfschwe
ROOT_EMAIL= root@root.com
#
SECRET=master45#*
# Utils
EMAIL_API_KEY=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
#
PUBLIC_DOMAIN=localhost:3000
# Code user valid account dd
CODE_LEN=40
# Password len
PASSWORD_LEN = 10