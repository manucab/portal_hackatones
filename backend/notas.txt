// TODO -- lowerCase insert y selelct

// SELECT * FROM `table` WHERE LOWER(`Value`) = LOWER("IAreSavage")

0. Email con reserva del code inscripcion al hackathon
1. welcom email
2. rotate files
3. Inscribirse a hackathon
